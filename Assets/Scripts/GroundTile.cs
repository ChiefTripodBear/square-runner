using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum TilePos { raised, neutral, lowered};

public class GroundTile : MonoBehaviour
{
    public TilePos tilePos;                                                         //capture if this tile is raised, or lowered
    
    void Start()
    {
        
    }

    void Update()
    {
        
    }
}
